# Pantheon-IaC — environment variables (TEMPLATE)
# Copy to `.env` and fill in the real path. `.env` is gitignored; this file is committed.

# Path to the Pantheon project notes in the Obsidian vault (project context for Claude Code).
PANTHEON_NOTES_PATH="/absolute/path/to/Notebook/Lab/Pantheon"
